# 性能预算审计平台 — 代码理解与梳理文档

## 1. 项目概述

本项目是一个 **性能预算 / 构建产物审计平台**，用于监控前端构建产物的体积变化，设定预算阈值并自动检测违规，支持快照上传、体积趋势分析、Chunk 级别对比与依赖溯源。

技术栈：

| 层级 | 技术 |
|------|------|
| 前端 | React 18 + React Router 6 + Recharts + Axios + Lucide Icons |
| 后端 | Express 4 + SQLite3 + JWT + bcryptjs |
| 构建 | Vite 5 |
| 数据库 | SQLite（文件型，`backend/database.db`） |

---

## 2. 目录结构

```
performance-budget-audit/
├── backend/
│   ├── server.js              # Express 入口，路由挂载，静态文件服务
│   ├── database.js            # SQLite 建表脚本，导出 db 实例
│   ├── middleware/
│   │   └── auth.js            # JWT 鉴权中间件 + SECRET_KEY
│   ├── routes/
│   │   ├── auth.js            # 登录 / 注册
│   │   ├── projects.js        # 项目 CRUD + 预算配置读写
│   │   ├── snapshots.js       # 快照上传 / 查询 / 对比 / 备注更新
│   │   └── dashboard.js       # 仪表盘统计 + 最大增长 Chunk
│   ├── package.json
│   └── package-lock.json
├── frontend/
│   ├── src/
│   │   ├── main.jsx           # React 挂载入口
│   │   ├── App.jsx            # 路由定义 + ProtectedRoute
│   │   ├── context/
│   │   │   └── AuthContext.jsx # 全局认证状态（token/user/login/logout/register）
│   │   ├── components/
│   │   │   └── Layout.jsx     # 侧边栏布局 + 导航
│   │   ├── pages/
│   │   │   ├── Login.jsx          # 登录 / 注册页
│   │   │   ├── Dashboard.jsx      # 仪表盘（统计卡片 + 趋势图 + 异常列表）
│   │   │   ├── Projects.jsx       # 项目列表 + 新建弹窗
│   │   │   ├── ProjectDetail.jsx  # 项目详情（预算配置 + 快照列表 + 上传构建）
│   │   │   ├── SnapshotDetail.jsx # 快照详情（Chunk 列表 + 违规 + 依赖树 + 备注）
│   │   │   └── CompareView.jsx    # 快照对比（差异图表 + 变更明细）
│   │   └── utils/
│   │       └── format.js      # formatBytes / formatDate 工具函数
│   ├── index.html
│   ├── vite.config.js         # Vite 配置 + API 代理
│   └── package.json
└── .gitignore
```

---

## 3. 数据库表结构与关系

### 3.1 ER 关系图

```
users (1) ──< projects (N)
projects (1) ──< budget_configs (1)
projects (1) ──< build_snapshots (N)
build_snapshots (1) ──< chunks (N)
build_snapshots (1) ──< budget_violations (N)
chunks (1) ──< budget_violations (0..1)
```

### 3.2 表字段详情

#### users

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | TEXT UNIQUE | 用户名，唯一 |
| password | TEXT | bcrypt 哈希密码 |
| created_at | DATETIME | 创建时间 |

#### projects

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| user_id | INTEGER FK→users.id | 所属用户 |
| name | TEXT | 项目名称 |
| description | TEXT | 项目描述 |
| created_at | DATETIME | 创建时间 |

#### budget_configs

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | INTEGER PK | | 自增主键 |
| project_id | INTEGER FK→projects.id | | 关联项目 |
| max_total_size | INTEGER | 5242880 (5MB) | 最大总体积（字节） |
| max_chunk_size | INTEGER | 1048576 (1MB) | 最大单 Chunk 体积（字节） |
| max_gzip_size | INTEGER | 524288 (512KB) | 最大 Gzip 总体积（字节） |
| created_at | DATETIME | | 创建时间 |

> **注意**：budget_configs 与 projects 是 1:1 关系，创建项目时自动插入默认预算配置行。

#### build_snapshots

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| id | INTEGER PK | | 自增主键 |
| project_id | INTEGER FK→projects.id | | 关联项目 |
| branch | TEXT | 'main' | Git 分支 |
| version | TEXT | | 版本号 |
| build_time | DATETIME | CURRENT_TIMESTAMP | 构建时间 |
| commit_hash | TEXT | | Git commit hash |
| notes | TEXT | | 备注 |
| gate_status | TEXT | 'pending' | 门禁状态：pending / passed / failed |
| total_size | INTEGER | | 原始总体积 |
| total_gzip_size | INTEGER | | Gzip 总体积 |

#### chunks

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| snapshot_id | INTEGER FK→build_snapshots.id | 关联快照 |
| name | TEXT | Chunk 文件名 |
| size | INTEGER | 原始体积 |
| gzip_size | INTEGER | Gzip 体积 |
| chunk_type | TEXT | Chunk 类型（asset / chunk 等） |
| dependency_source | TEXT | 依赖来源路径 |

#### budget_violations

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| snapshot_id | INTEGER FK→build_snapshots.id | 关联快照 |
| chunk_id | INTEGER FK→chunks.id (可空) | 关联 Chunk（仅 chunk_size 违规时有值） |
| violation_type | TEXT | 违规类型：total_size / total_gzip_size / chunk_size |
| message | TEXT | 违规描述 |
| threshold | INTEGER | 阈值 |
| actual_value | INTEGER | 实际值 |
| created_at | DATETIME | 创建时间 |

---

## 4. 登录态与权限校验逻辑

### 4.1 认证流程

```
用户提交 username + password
        │
        ▼
┌─── POST /api/auth/login ───┐
│  1. 查询 users 表           │
│  2. bcrypt.compareSync 校验 │
│  3. jwt.sign 生成 token     │
│     payload: { userId, username } │
│     expiresIn: '7d'         │
│  4. 返回 { token, user }    │
└────────────────────────────┘
        │
        ▼
前端 AuthContext 存储：
  - localStorage.token
  - localStorage.user (JSON)
  - axios.defaults.headers.common['Authorization'] = `Bearer ${token}`
```

### 4.2 鉴权中间件

- 位置：`backend/middleware/auth.js`
- 逻辑：从 `Authorization: Bearer <token>` 提取 JWT，验证签名与有效期
- 成功：将 `req.userId` 和 `req.username` 注入请求上下文
- 失败：返回 401
- **应用范围**：projects、snapshots、dashboard 三个路由模块均通过 `router.use(authMiddleware)` 全局拦截；auth 路由无需鉴权

### 4.3 前端路由守卫

- `App.jsx` 中的 `ProtectedRoute` 组件：检查 `AuthContext.user` 是否存在，不存在则 `<Navigate to="/login" />`
- 已登录用户访问 `/login` 时自动跳转首页

### 4.4 数据隔离

- 所有数据查询均带 `WHERE user_id = req.userId` 条件，实现用户级数据隔离
- **注意**：snapshots 路由的 `GET /:id` 和 `GET /:id/compare/:compareId` 未校验快照是否属于当前用户，存在越权访问风险

---

## 5. 核心接口说明

### 5.1 认证接口

| 方法 | 路径 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| POST | /api/auth/register | `{ username, password }` | `{ token, user: { id, username } }` | 注册并自动登录 |
| POST | /api/auth/login | `{ username, password }` | `{ token, user: { id, username } }` | 登录 |

### 5.2 项目接口

| 方法 | 路径 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| GET | /api/projects | — | `[{ id, user_id, name, description, created_at }]` | 获取当前用户所有项目 |
| POST | /api/projects | `{ name, description }` | `{ id, name, description, user_id }` | 创建项目（自动创建默认预算配置） |
| GET | /api/projects/:id | — | `{ id, user_id, name, description, created_at }` | 获取项目详情（含 user_id 校验） |
| GET | /api/projects/:id/budget | — | `{ id, project_id, max_total_size, max_chunk_size, max_gzip_size }` | 获取预算配置 |
| PUT | /api/projects/:id/budget | `{ max_total_size, max_chunk_size, max_gzip_size }` | `{ success: true }` | 更新预算配置 |

### 5.3 快照接口

| 方法 | 路径 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| POST | /api/snapshots | 见下方详细说明 | `{ id, gate_status, violations }` | 上传构建快照 |
| GET | /api/snapshots/project/:projectId | `?branch=&version=&limit=20` | `[{ id, project_id, branch, version, ... }]` | 获取项目快照列表 |
| GET | /api/snapshots/:id | — | `{ ...snapshot, chunks, violations }` | 获取快照详情（含 chunks + violations） |
| GET | /api/snapshots/:id/compare/:compareId | — | 见下方对比接口返回 | 两个快照对比 |
| PUT | /api/snapshots/:id/notes | `{ notes }` | `{ success: true }` | 更新快照备注 |

#### POST /api/snapshots 入参详情

```json
{
  "project_id": 1,
  "branch": "main",
  "version": "v1.0.0",
  "commit_hash": "abc123",
  "notes": "release note",
  "build_time": "2024-01-01T00:00:00",
  "chunks": [
    {
      "name": "main.js",
      "size": 102400,
      "gzip_size": 30000,
      "chunk_type": "asset",
      "dependency_source": "node_modules/lodash"
    }
  ]
}
```

#### GET /api/snapshots/:id/compare/:compareId 返回详情

```json
{
  "snapshot1": { "...snapshot", "chunks": [...] },
  "snapshot2": { "...snapshot", "chunks": [...] },
  "changes": [
    {
      "name": "main.js",
      "status": "modified | added | removed",
      "old_size": 102400,
      "new_size": 110000,
      "size_diff": 7600,
      "old_gzip_size": 30000,
      "new_gzip_size": 32000,
      "gzip_diff": 2000
    }
  ],
  "total_diff": 7600,
  "total_gzip_diff": 2000
}
```

### 5.4 仪表盘接口

| 方法 | 路径 | 入参 | 返回 | 说明 |
|------|------|------|------|------|
| GET | /api/dashboard/stats | — | 见下方 | 统计概览 |
| GET | /api/dashboard/max-growth | `?project_id=` | 见下方 | 最大增长 Chunk Top10 |

#### GET /api/dashboard/stats 返回

```json
{
  "total_snapshots": 42,
  "failed_count": 5,
  "violation_count": 12,
  "recent_failed": [{ "...最近5条失败快照" }],
  "size_trend": [
    { "date": "2024-01-01", "version": "v1.0", "size": 1024000, "gzip_size": 300000, "project": "my-app" }
  ]
}
```

#### GET /api/dashboard/max-growth 返回

```json
[
  {
    "name": "main.js",
    "current_size": 50000,
    "prev_size": 30000,
    "growth": 20000,
    "version": "v2.0",
    "project_name": "my-app"
  }
]
```

---

## 6. 预算校验规则

快照上传时自动触发预算校验，逻辑位于 `backend/routes/snapshots.js` 的 `checkBudgetViolations` 函数：

| 违规类型 | 判断条件 | 默认阈值 | 说明 |
|----------|----------|----------|------|
| `total_size` | 所有 Chunk 的 `size` 之和 > `max_total_size` | 5MB (5242880) | 原始总体积超限 |
| `total_gzip_size` | 所有 Chunk 的 `gzip_size` 之和 > `max_gzip_size` | 512KB (524288) | Gzip 总体积超限 |
| `chunk_size` | 单个 Chunk 的 `gzip_size` > `max_chunk_size` | 1MB (1048576) | 单 Chunk Gzip 体积超限 |

**门禁判定**：存在任一违规 → `gate_status = 'failed'`；无违规 → `gate_status = 'passed'`

**校验流程**：

```
上传快照 → 插入 build_snapshots 记录 → 逐个插入 chunks 记录
    → 收集所有 chunkIds → 调用 checkBudgetViolations(snapshotId, chunkIds, budgetConfig)
    → 判定 gate_status → UPDATE build_snapshots SET gate_status
    → 逐条 INSERT budget_violations → 返回结果
```

---

## 7. 快照存储与对比逻辑

### 7.1 快照存储

1. 前端提交 JSON 格式的构建清单（支持 webpack stats / rollup stats / 自定义数组）
2. 前端对字段做兼容映射：`name ← name | fileName | chunkNames[0]`，`gzip_size ← gzipSize | gzip_size | size * 0.3`
3. 后端计算 `total_size` 和 `total_gzip_size`，插入 `build_snapshots` 表
4. 逐条插入 `chunks` 表
5. 执行预算校验，写入 `budget_violations`

### 7.2 快照对比逻辑

对比接口 `GET /api/snapshots/:id/compare/:compareId` 的核心算法：

1. 分别获取两个快照及其 chunks
2. 以 Chunk `name` 为 key 建立 Map
3. 遍历所有 Chunk 名称的并集：
   - 两个快照都有 → `status: 'modified'`，计算 size_diff 和 gzip_diff
   - 仅快照1有 → `status: 'added'`
   - 仅快照2有 → `status: 'removed'`
4. 计算总体积差异：`total_diff = snapshot1.total_size - snapshot2.total_size`

**注意**：对比以 Chunk name 为匹配键，若同一 Chunk 在不同构建中改名则会被误判为"删除+新增"。

---

## 8. 页面与接口数据流

### 8.1 页面路由

| 路由 | 页面组件 | 数据来源接口 |
|------|----------|-------------|
| /login | Login | POST /api/auth/login, POST /api/auth/register |
| / | Dashboard | GET /api/dashboard/stats, GET /api/dashboard/max-growth |
| /projects | Projects | GET /api/projects, POST /api/projects |
| /projects/:id | ProjectDetail | GET /api/projects/:id, GET /api/projects/:id/budget, PUT /api/projects/:id/budget, GET /api/snapshots/project/:projectId, POST /api/snapshots |
| /snapshots/:id | SnapshotDetail | GET /api/snapshots/:id, GET /api/snapshots/project/:projectId, PUT /api/snapshots/:id/notes |
| /compare/:snapshotId/:compareId | CompareView | GET /api/snapshots/:snapshotId/compare/:compareId |

### 8.2 核心数据流图

```
┌──────────┐     注册/登录      ┌──────────────┐
│  Login   │ ──────────────────►│  /api/auth   │
│  页面    │ ◄──────────────────│  register/login│
└──────────┘    token + user    └──────────────┘

┌──────────┐     获取统计       ┌──────────────┐
│Dashboard │ ──────────────────►│ /api/dashboard│
│  页面    │ ◄──────────────────│  stats        │
│          │ ──────────────────►│  max-growth   │
└──────────┘    统计数据        └──────────────┘

┌──────────┐     项目列表       ┌──────────────┐
│ Projects │ ──────────────────►│ /api/projects│
│  页面    │ ◄──────────────────│  GET/POST    │
└──────────┘    项目数据        └──────────────┘

┌──────────────┐  项目详情+快照列表+预算  ┌─────────────────┐
│ProjectDetail │ ──────────────────────►│ /api/projects/:id│
│    页面      │ ◄──────────────────────│ /api/projects/:id/budget│
│              │ ──────────────────────►│ /api/snapshots/project/:id│
│              │ ◄──────────────────────│ /api/snapshots POST│
└──────────────┘    快照+预算数据       └─────────────────┘

┌───────────────┐  快照详情+违规  ┌──────────────────┐
│SnapshotDetail │ ─────────────►│ /api/snapshots/:id│
│    页面       │ ◄─────────────│  GET / PUT notes  │
└───────────────┘               └──────────────────┘

┌──────────────┐  对比数据      ┌──────────────────────────┐
│ CompareView  │ ─────────────►│ /api/snapshots/:id/compare│
│    页面      │ ◄─────────────│  /:compareId              │
└──────────────┘               └──────────────────────────┘
```

---

## 9. 核心业务流程

### 9.1 构建产物审计完整流程

```
1. 用户登录 → 获取 JWT Token
2. 创建项目 → 自动生成默认预算配置
3. 配置预算阈值（可选，修改 max_total_size / max_chunk_size / max_gzip_size）
4. 上传构建产物 JSON → 系统自动：
   a. 解析 chunks 列表
   b. 计算 total_size / total_gzip_size
   c. 插入 build_snapshots + chunks 记录
   d. 执行预算校验 → 生成 budget_violations
   e. 判定 gate_status（passed / failed）
5. 查看快照详情 → Chunk 列表 + 违规信息 + 依赖体积树
6. 选择两个快照对比 → 体积差异图表 + 变更明细
7. 仪表盘查看全局统计 → 趋势图 + 异常构建 + 最大增长 Chunk
```

### 9.2 门禁状态流转

```
pending（默认） ──上传完成校验后──► passed（无违规）或 failed（有违规）
```

> **注意**：当前门禁状态仅在快照上传时计算一次，不支持修改预算后重新校验历史快照。

---

## 10. 注意点与维护建议

### 10.1 安全问题

| 问题 | 位置 | 严重程度 | 说明 |
|------|------|----------|------|
| JWT 密钥硬编码 | `backend/middleware/auth.js` | 🔴 高 | `SECRET_KEY = 'performance-budget-secret-key-2024'` 硬编码在源码中，应使用环境变量 |
| 快照越权访问 | `backend/routes/snapshots.js` GET /:id | 🔴 高 | 未校验快照是否属于当前用户，任何登录用户可通过遍历 id 查看他人快照 |
| 对比越权访问 | `backend/routes/snapshots.js` GET /:id/compare/:compareId | 🔴 高 | 同上，对比接口也未做归属校验 |
| 备注越权修改 | `backend/routes/snapshots.js` PUT /:id/notes | 🟡 中 | 未校验快照归属即可修改备注 |
| 无密码强度校验 | `backend/routes/auth.js` | 🟡 中 | 注册时未限制密码最小长度和复杂度 |
| 无速率限制 | 全局 | 🟡 中 | 登录/注册接口无 rate-limit，存在暴力破解风险 |
| multer 未使用 | `backend/package.json` | 🟢 低 | 依赖中声明了 multer 但代码中未使用 |

### 10.2 代码质量问题

| 问题 | 位置 | 说明 |
|------|------|------|
| 回调地狱 | `backend/routes/snapshots.js` POST / | 快照上传逻辑嵌套多层回调（db.run 嵌套 + forEach 内异步），可读性差且错误处理不完整 |
| 违规写入无等待 | `backend/routes/snapshots.js` L97-103 | `violations.forEach(v => db.run(...))` 未等待所有 INSERT 完成就已返回响应，若写入失败前端无感知 |
| chunkIds 在错误分支中可能不完整 | `backend/routes/snapshots.js` L88-90 | 当某个 chunk INSERT 出错时，`chunkIds` 缺少该项但仍继续校验，结果可能不准确 |
| 预算配置无项目归属校验 | `backend/routes/projects.js` PUT /:id/budget | 未验证 project_id 是否属于当前用户 |
| 前端无全局错误拦截 | `frontend/src/` | Axios 未配置响应拦截器处理 401 自动跳转登录 |
| 用户信息从 localStorage 恢复 | `frontend/src/context/AuthContext.jsx` | 仅从 localStorage 读取 user JSON，未向服务端验证 token 有效性，token 过期后仍可能显示已登录状态 |

### 10.3 架构与扩展性问题

| 问题 | 说明 |
|------|------|
| SQLite 并发限制 | SQLite 不支持高并发写入，多用户同时上传快照可能锁库 |
| 无数据库迁移机制 | 建表语句用 `CREATE TABLE IF NOT EXISTS`，后续表结构变更无版本管理 |
| 预算校验不可重跑 | 修改预算配置后，历史快照的 gate_status 不会自动更新 |
| 无分页支持 | 快照列表接口仅支持 `LIMIT`，无 offset 分页，数据量大时性能下降 |
| 无项目删除功能 | 缺少 DELETE /api/projects/:id 和级联删除逻辑 |
| 无快照删除功能 | 缺少 DELETE /api/snapshots/:id |
| 前端无状态管理库 | 全部使用组件内 useState，若后续功能扩展可能需要引入状态管理 |
| 无 CI/CD 集成 | 当前仅支持手动上传 JSON，未提供 CLI 工具或 CI 插件 |
| 对比逻辑基于 Chunk name | 若构建工具生成 hash 文件名（如 main.abc123.js），同名匹配会失效 |

### 10.4 维护建议

1. **安全加固**：将 JWT 密钥迁移到环境变量；为快照/对比/备注接口增加用户归属校验；添加登录速率限制
2. **重构回调**：将 `snapshots.js` 的 POST 逻辑改为 async/await 或 Promise 链，确保违规写入完成后再返回响应
3. **添加 Axios 拦截器**：全局拦截 401 响应，自动清除 token 并跳转登录页
4. **Token 有效性验证**：前端启动时调用一个验证接口（如 GET /api/auth/me）确认 token 仍有效
5. **数据库迁移**：引入 knex 或 drizzle 等 ORM，支持 migration 管理
6. **预算重校验**：提供"重新校验"按钮，修改预算后可触发对历史快照的重新审计
7. **CI 集成**：提供命令行工具或 GitHub Action，在 CI 流程中自动上传构建产物
8. **Chunk 匹配优化**：对比时支持模糊匹配或忽略 hash 后缀，提高对比准确性
9. **分页与排序**：为快照列表接口添加 offset/limit 分页和排序参数
10. **删除功能**：实现项目和快照的软删除，级联清理关联数据

---

## 11. 模块关系总览

```
┌─────────────────────────────────────────────────────────┐
│                     Frontend (React)                     │
│                                                          │
│  AuthContext ──────► ProtectedRoute ──────► Layout       │
│       │                                       │          │
│       ▼                                       ▼          │
│    Login                              ┌─────────────┐   │
│                                      │   Dashboard  │   │
│                                      └─────────────┘   │
│                                      ┌─────────────┐   │
│                                      │   Projects   │   │
│                                      └─────────────┘   │
│                                      ┌─────────────┐   │
│                                      │ProjectDetail │   │
│                                      └─────────────┘   │
│                                      ┌─────────────┐   │
│                                      │SnapshotDetail│   │
│                                      └─────────────┘   │
│                                      ┌─────────────┐   │
│                                      │ CompareView  │   │
│                                      └─────────────┘   │
└──────────────────────────┬──────────────────────────────┘
                           │ Axios (Bearer Token)
                           ▼
┌──────────────────────────────────────────────────────────┐
│                    Backend (Express)                      │
│                                                           │
│  authMiddleware ◄──── projects ──────► budget_configs     │
│       │               │                                   │
│       ▼               ▼                                   │
│     auth.js      snapshots ──────► chunks                 │
│                       │                │                   │
│                       ▼                ▼                   │
│               budget_violations ◄────┘                    │
│                       │                                   │
│                       ▼                                   │
│                   dashboard                               │
└──────────────────────────┬───────────────────────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   SQLite    │
                    │ database.db │
                    └─────────────┘
```
