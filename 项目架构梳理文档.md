# 性能预算审计平台 - 项目架构梳理文档

## 1. 项目概述

本项目是一个**性能预算/构建产物审计平台**，用于对前端构建产物进行体积监控、预算管理和趋势分析。主要功能包括：

- 用户登录认证与注册
- 项目管理
- 预算配置（总体积、单 Chunk 体积、Gzip 体积）
- 构建快照上传与管理
- Chunk 体积分析
- 预算违规检测与门禁判定
- 体积趋势统计图表
- 异常构建展示
- 不同快照之间的体积对比

## 2. 技术栈

### 2.1 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | - | 运行环境 |
| Express | ^4.18.2 | Web 框架 |
| SQLite3 | ^5.1.6 | 数据库 |
| bcryptjs | ^2.4.3 | 密码加密 |
| jsonwebtoken | ^9.0.2 | JWT 认证 |
| cors | ^2.8.5 | 跨域支持 |
| multer | ^1.4.5-lts.1 | 文件上传（当前未深度使用） |
| nodemon | ^3.0.1 | 开发热重载 |

### 2.2 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | ^18.2.0 | UI 框架 |
| React Router DOM | ^6.20.0 | 路由管理 |
| Recharts | ^2.10.3 | 图表库 |
| Axios | ^1.6.2 | HTTP 请求 |
| Lucide React | ^0.294.0 | 图标库 |
| Vite | ^5.0.4 | 构建工具 |
| @vitejs/plugin-react | ^4.2.0 | React 插件 |

## 3. 目录结构

```
performance-budget-audit/
├── backend/                 # 后端服务
│   ├── middleware/          # 中间件
│   │   └── auth.js          # JWT 认证中间件
│   ├── routes/              # API 路由
│   │   ├── auth.js          # 认证相关接口
│   │   ├── dashboard.js     # 仪表盘接口
│   │   ├── projects.js      # 项目管理接口
│   │   └── snapshots.js     # 快照管理接口
│   ├── database.js          # 数据库初始化与连接
│   ├── server.js            # 服务入口文件
│   ├── package.json
│   └── database.db          # SQLite 数据库文件（运行时生成）
│
└── frontend/                # 前端应用
    ├── src/
    │   ├── components/      # 公共组件
    │   │   └── Layout.jsx   # 布局组件（侧边栏 + 内容区）
    │   ├── context/         # React Context
    │   │   └── AuthContext.jsx  # 认证状态管理
    │   ├── pages/           # 页面组件
    │   │   ├── Login.jsx         # 登录/注册页
    │   │   ├── Dashboard.jsx     # 仪表盘页
    │   │   ├── Projects.jsx      # 项目列表页
    │   │   ├── ProjectDetail.jsx # 项目详情页
    │   │   ├── SnapshotDetail.jsx # 快照详情页
    │   │   └── CompareView.jsx   # 快照对比页
    │   ├── utils/           # 工具函数
    │   │   └── format.js    # 格式化工具（字节、日期）
    │   ├── App.jsx          # 应用根组件（路由配置）
    │   └── main.jsx         # 应用入口
    ├── index.html
    ├── vite.config.js
    └── package.json
```

## 4. 数据库设计

数据库使用 **SQLite3**，共包含 **6 张表**。

### 4.1 表结构总览

| 表名 | 说明 | 关联关系 |
|------|------|----------|
| users | 用户表 | - |
| projects | 项目表 | 关联 users |
| budget_configs | 预算配置表 | 关联 projects |
| build_snapshots | 构建快照表 | 关联 projects |
| chunks | Chunk表 | 关联 build_snapshots |
| budget_violations | 预算违规表表表 | 关联 build_snapshots、chunks |

### 4.2 表详细结构

#### 4.2.1 users 用户表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用户ID |
| username | TEXT | UNIQUE NOT NULL | 用户名 |
| password | TEXT | NOT NULL | 加密后的密码 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.2.2 projects 项目表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 项目ID |
| user_id | INTEGER | NOT NULL, FOREIGN KEY | 所属用户ID |
| name | TEXT | NOT NULL | 项目名称 |
| description | TEXT | - | 项目描述 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.2.3 budget_configs 预算配置表

每个项目对应一条预算配置记录。

| 字段名 | 类型 | 约束 | 说明 | 默认值 |
|--------|------|------|------|--------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 配置ID | - |
| project_id | INTEGER | NOT NULL, FOREIGN KEY | 项目ID | - |
| max_total_size | INTEGER | - | 最大总体积（字节） | 5242880 (5MB) |
| max_chunk_size | INTEGER | - | 最大单Chunk体积表表（字节） | 1048576 (1MB) |
| max_gzip_size | INTEGER | - | 最大Gzip总体积表表表表（字节） | 524288 (512KB) |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 | - |

#### 4.2.4 build_snapshots 构建快照表

| 字段名 | 类型 | 约束 | 说明 | 默认值 |
|--------|------|------|------|--------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 快照ID | - |
| project_id | INTEGER | NOT NULL, FOREIGN KEY | 项目ID | - |
| branch | TEXT | - | 分支名 | 'main' |
| version | TEXT | NOT NULL | 版本号 | - |
| build_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 构建时间 | - |
| commit_hash | TEXT | - | Commit Hash | - |
| notes | TEXT | - | 备注信息 | - |
| gate_status | TEXT | - | 门禁状态（pending/passed/failed） | 'pending' |
| total_size | INTEGER | NOT NULL | 总体积表表表（字节） | - |
| total_gzip_size | INTEGER | NOT NULL | Gzip总体积表表表（字节） | - |

#### 4.2.5 chunks Chunk表

每个快照包含多个 Chunk 表表表表。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | Chunk ID |
| snapshot_id | INTEGER | NOT NULL, FOREIGN KEY | 所属快照ID |
| name | TEXT | NOT NULL | Chunk名称 |
| size | INTEGER | NOT NULL | 原始体积表表表表（字节） |
| gzip_size | INTEGER | NOT NULL | Gzip体积表表表表（字节） |
| chunk_type | TEXT | - | Chunk类型表表表 |
| dependency_source | TEXT | - | 依赖来源表表表路径 |

#### 4.2.6 budget_violations 预算违规表表表表

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 违规记录ID |
| snapshot_id | INTEGER | NOT NULL, FOREIGN KEY | 快照ID |
| chunk_id | INTEGER | FOREIGN KEY | 关联的Chunk表表表ID（可为空） |
| violation_type | TEXT | NOT NULL | 违规类型表表表表 |
| message | TEXT | NOT NULL | 违规信息表表表表 |
| threshold | INTEGER | - | 阈值表表表表（字节） |
| actual_value | INTEGER | - | 实际值表表表表（字节） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.3 表关系图

```
users (1) ────< projects (1) ────< budget_configs
                    │
                    └────< build_snapshots (N)
                               │
                               ├────< chunks (N)
                               │
                               └────< budget_violations (N)
                                           │
chunks ────────────────────────────────────┘
```

## 5. 前端架构

### 5.1 路由结构

| 路径 | 页面组件 | 说明 | 是否需要登录 |
|------|----------|------|-------------|
| /login | Login | 登录/注册页 | 否 |
| / | Dashboard | 仪表盘 | 是 |
| /projects | Projects | 项目列表表表页 | 是 |
| /projects/:id | ProjectDetail | 项目详情表表页 | 是 |
| /snapshots/:id | SnapshotDetail | 快照详情表表页 | 是 |
| /compare/:snapshotId/:compareId | CompareView | 快照对比页 | 是 |

路由配置见 [App.jsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/frontend/src/App.jsx)。

### 5.2 状态管理

使用 **React Context** 进行全局状态管理，主要是认证状态。

#### AuthContext 提供的状态和方法

| 状态/方法 | 类型 | 说明 |
|-----------|------|------|
| user | Object/null | 当前登录用户信息表表表表 |
| token | String/null | JWT Token |
| loading | Boolean | 认证加载状态表表表表 |
| login(username, password) | Function | 登录方法 |
| register(username, password) | Function | 注册方法 |
| logout() | Function | 登出方法 |

Token 和用户信息表表表表存储在 **localStorage** 中，key 分别为 `token` 和 `user`。

详见 [AuthContext.jsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/frontend/src/context/AuthContext.jsx)。

### 5.3 页面说明

#### 5.3.1 Login 登录页
- 支持登录/注册切换
- 用户名密码表单提交
- 登录成功后跳转首页
- 已登录用户自动重定向到首页

#### 5.3.2 Dashboard 仪表盘页
- 统计卡片：总构建次数、超预算次数、违规项总数
- 体积趋势折线图（原始体积表表表表 + Gzip 体积表表表表）
- 最大增长 Chunk 柱状图
- 最近异常构建列表

#### 5.3.3 Projects 项目列表表表页
- 项目卡片列表表表表表
- 新建项目弹窗
- 点击卡片进入项目详情表页

#### 5.3.4 ProjectDetail 项目详情表页
- 项目基本信息表表表表
- 预算配置表表表表展示卡片
- 构建历史表表表表表格（支持按分支、版本筛选）
- 上传构建产物弹窗（支持 JSON 文件上传或粘贴）
- 预算配置表表表表编辑弹窗

#### 5.3.5 SnapshotDetail 快照详情表页
- 快照基本信息表表表表
- 体积统计卡片（原始、Gzip、Chunk表表表表数量、违规数量）
- 预算违规表表表表展示区
- Chunk 列表表表表（可展开查看详情）
- 依赖体积树表表表表（如果有 dependency_source 数据）
- 备注信息表表表表（可编辑）
- 对比版本选择器

#### 5.3.6 CompareView 快照对比页
- 总体积差异、Gzip 差异、新增/删除 Chunk 数量表表表表
- 体积变化柱状图（前 15 个变化最大的 Chunk）
- 变更明细表格（支持按新增/删除/修改筛选）

### 5.4 工具函数

[format.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/frontend/src/utils/format.js) 提供两个工具函数：

- `formatBytes(bytes)`：将字节数格式化为可读字符串（B/KB/MB/GB）
- `formatDate(dateStr)`：将日期字符串格式化为中文日期格式表表表表

## 6. 后端架构

### 6.1 服务入口

[server.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/server.js) 是后端入口文件，主要职责：

- 初始化 Express 应用
- 配置中间件（CORS、JSON 解析）
- 初始化数据库连接
- 挂载路由表
- 静态文件服务（前端构建产物）
- 启动服务（默认端口 8038）

### 6.2 路由模块表表表表

| 路由表前缀 | 文件 | 说明 | 是否需要认证 |
|-----------|------|------|-------------|
| /api/auth | auth.js | 认证相关接口 | 否 |
| /api/projects | projects.js | 项目管理相关接口 | 是 |
| /api/snapshots | snapshots.js | 快照管理相关接口 | 是 |
| /api/dashboard | dashboard.js | 仪表盘相关接口 | 是 |

### 6.3 中间件

#### 认证中间件表表表表 [auth.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/middleware/auth.js)

- 从 `Authorization: Bearer <token>` 请求头中提取 Token
- 使用 JWT 验证 Token 有效性
- 验证通过后将 `userId` 和 `username` 挂载到 `req` 对象上
- 验证失败返回 401 状态码

> **注意**：JWT 密钥 `performance-budget-secret-key-2024` 硬编码在代码中，生产环境应通过环境变量配置。

## 7. 认证与权限校验逻辑

### 7.1 注册流程

1. 用户提交用户名和密码
2. 后端使用 **bcryptjs** 对密码进行哈希加密（salt 轮数：10）
3. 将用户信息表表表表插入 users 表
4. 生成 JWT Token（有效期 7 天）
5. 返回 Token 和用户信息表表表表
6. 前端将 Token 和用户信息表表表表存入 localStorage

### 7.2 登录流程

1. 用户提交用户名和密码
2. 后端根据用户名查询用户
3. 使用 bcryptjs 比较密码哈希
4. 验证通过后生成 JWT Token
5. 返回 Token 和用户信息表表表表
6. 前端将 Token 和用户信息表表表表存入 localStorage
7. 后续请求自动在请求头携带 `Authorization: Bearer <token>`

### 7.3 权限校验逻辑

- 所有 `/api/projects`、`/api/snapshots`、`/api/dashboard` 下的接口都需要认证
- 项目级数据通过 `user_id` 进行隔离，查询时会校验 `user_id` 是否与当前登录用户一致
- 快照表和 Chunk 表表表表数据通过项目表关联进行权限控制（但目前快照详情接口未显式校验用户权限，存在越权风险）

## 8. 核心业务流程

### 8.1 创建项目流程

1. 用户填写项目名称和描述
2. 调用 `POST /api/projects` 接口
3. 后端插入 projects 表记录
4. 同时自动创建对应的 budget_configs 表记录（使用默认值）
5. 返回项目信息表表表表

### 8.2 预算配置表表表表流程

1. 用户进入项目详情页，自动加载预算配置表表表表
2. 点击"预算配置表表表表"按钮，弹窗编辑预算表表表表
3. 点击保存，调用 `PUT /api/projects/:id/budget` 接口
4. 后端更新 budget_configs 表表表表
5. 前端刷新预算表表表表展示

**默认预算值：**
- max_total_size: 5,242,880 字节（5MB）
- max_chunk_size: 1,048,576 字节（1MB）
- max_gzip_size: 524,288 字节（512KB）

### 8.3 构建快照上传流程

这是系统最核心的业务流程。

#### 8.3.1 前端处理流程

1. 用户点击"上传构建"按钮
2. 填写版本号、分支、Commit Hash、构建时间、备注等信息表表表表
3. 上传 JSON 文件或直接粘贴 JSON 内容表表表表
4. 前端解析 JSON，兼容多种格式表表表表：
   - 数组格式：直接使用
   - 对象格式：取 `chunks` 或 `assets` 字段表表表表
5. 对每个 Chunk 进行字段映射表表表表：
   - name: `name` / `fileName` / `chunkNames[0]` / 'unknown'
   - size: `size` / 0
   - gzip_size: `gzipSize` / `gzip_size` / size * 0.3（估算）
   - chunk_type: `type` / `chunkType` / 'asset'
   - dependency_source: `dependencySource` / `dependency_source` / ''
6. 调用 `POST /api/snapshots` 接口提交数据

#### 8.3.2 后端处理流程

1. 接收快照数据和 Chunk 列表表表表表
2. 计算总体积表表表表和 Gzip 总体积表表表表
3. 查询项目的预算配置表表表表
4. 插入 build_snapshots 表表表记录
5. 循环插入所有 chunks 表表表表记录
6. 调用 `checkBudgetViolations` 函数进行预算校验
7. 根据违规数量设置 gate_status（有违规：failed，无违规：passed）
8. 更新 build_snapshots 的 gate_status 表表表表字段
9. 插入所有违规记录到 budget_violations 表表表表
10. 返回快照ID、门禁状态、违规列表表表表表

### 8.4 预算校验规则

预算校验逻辑在 [snapshots.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/routes/snapshots.js) 的 `checkBudgetViolations` 函数中表表表表。

校验三类违规表表表表：

| 违规类型 | 校验规则 | violation_type 表表表表 |
|----------|----------|---------------------|
| 总体积表表表表超限 | total_size > max_total_size 表表表表 | total_size |
| Gzip 总体积表表表表超限 | total_gzip_size > max_gzip_size 表表表表 | total_gzip_size |
| 单 Chunk 体积表表表表超限 | chunk.gzip_size > max_chunk_size 表表表表 | chunk_size |

> **注意**：单 Chunk 体积超限校验的是 **Gzip 体积表表表表**，不是原始体积表表表表。

### 8.5 快照对比流程

1. 用户在快照详情页选择对比版本表表表表
2. 点击"对比"按钮，跳转到对比页
3. 调用 `GET /api/snapshots/:id/compare/:compareId` 接口
4. 后端分别查询两个快照表及其 chunks 表表表表数据
5. 通过 name 字段进行匹配表表表表，生成变更列表表表表表
6. 变更状态分为三类：added（新增）、removed（删除）、modified（修改）
7. 计算总体差异表表表表
8. 返回对比结果
9. 前端展示差异图表和明细表格

### 8.6 仪表盘数据聚合流程

1. 调用 `GET /api/dashboard/stats` 获取统计数据
2. 调用 `GET /api/dashboard/max-growth` 获取增长最快的 Chunk 表表表表
3. 统计数据包括：
   - total_snapshots：总构建次数
   - failed_count：失败次数
   - violation_count：违规次数
   - recent_failed：最近失败的 5 个快照
   - size_trend：最近 20 次构建的体积表表表表趋势数据

## 9. 关键接口说明

### 9.1 认证接口

#### 9.1.1 POST /api/auth/register

**入参：**
```json
{
  "username": "string",
  "password": "string"
}
```

**返回：**
```json
{
  "token": "jwt_token",
  "user": {
    "id": 1,
    "username": "string"
  }
}
```

#### 9.1.2 POST /api/auth/login

**入参：**
```json
{
  "username": "string",
  "password": "string"
}
```

**返回：** 同注册接口

### 9.2 项目管理接口

#### 9.2.1 GET /api/projects

获取当前用户的项目列表表表表表。

**返回：**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "name": "项目名",
    "description": "描述",
    "created_at": "2024-01-01 00:00:00"
  }
]
```

#### 9.2.2 POST /api/projects

创建新项目。

**入参：**
```json
{
  "name": "string",
  "description": "string"
}
```

**返回：**
```json
{
  "id": 1,
  "name": "string",
  "description": "string",
  "user_id": 1
}
```

#### 9.2.3 GET /api/projects/:id

获取项目详情表表表表。

**返回：** 项目对象

#### 9.2.4 GET /api/projects/:id/budget

获取项目预算配置表表表表。

**返回：**
```json
{
  "id": 1,
  "project_id": 1,
  "max_total_size": 5242880,
  "max_chunk_size": 1048576,
  "max_gzip_size": 524288,
  "created_at": "2024-01-01 00:00:00"
}
```

#### 9.2.5 PUT /api/projects/:id/budget

更新预算配置表表表表。

**入参：**
```json
{
  "max_total_size": 5242880,
  "max_chunk_size": 1048576,
  "max_gzip_size": 524288
}
```

**返回：**
```json
{
  "success": true
}
```

### 9.3 快照管理接口

#### 9.3.1 POST /api/snapshots

上传构建快照表表表表。

**入参：**
```json
{
  "project_id": 1,
  "branch": "main",
  "version": "v1.0.0",
  "commit_hash": "abc123",
  "notes": "备注",
  "build_time": "2024-01-01T00:00:00",
  "chunks": [
    {
      "name": "main.js",
      "size": 102400,
      "gzip_size": 30000,
      "chunk_type": "asset",
      "dependency_source": "src/main.js"
    }
  ]
}
```

**返回：**
```json
{
  "id": 1,
  "gate_status": "passed",
  "violations": [
    {
      "snapshotId": 1,
      "violation_type": "chunk_size",
      "message": "Chunk main.js exceeds size limit",
      "threshold": 1048576,
      "actual_value": 2000000
    }
  ]
}
```

#### 9.3.2 GET /api/snapshots/project/:projectId

获取项目的快照列表表表表表。

**Query 参数：**
- branch: 分支筛选表表表表
- version: 版本筛选表表表表
- limit: 返回数量表表表表（默认 20）

**返回：** 快照对象数组

#### 9.3.3 GET /api/snapshots/:id

获取快照详情表表表表（包含 chunks 和 violations）。

**返回：**
```json
{
  "id": 1,
  "project_id": 1,
  "branch": "main",
  "version": "v1.0.0",
  "build_time": "2024-01-01 00:00:00",
  "commit_hash": "abc123",
  "notes": "备注",
  "gate_status": "passed",
  "total_size": 102400,
  "total_gzip_size": 30000,
  "chunks": [...],
  "violations": [...]
}
```

#### 9.3.4 GET /api/snapshots/:id/compare/:compareId

对比两个快照。

**返回：**
```json
{
  "snapshot1": { ... },
  "snapshot2": { ... },
  "changes": [
    {
      "name": "main.js",
      "status": "modified",
      "old_size": 100000,
      "new_size": 120000,
      "size_diff": 20000,
      "old_gzip_size": 30000,
      "new_gzip_size": 35000,
      "gzip_diff": 5000
    }
  ],
  "total_diff": 20000,
  "total_gzip_diff": 5000
}
```

#### 9.3.5 PUT /api/snapshots/:id/notes

更新快照备注。

**入参：**
```json
{
  "notes": "string"
}
```

**返回：**
```json
{
  "success": true
}
```

### 9.4 仪表盘接口

#### 9.4.1 GET /api/dashboard/stats

获取仪表盘统计数据。

**返回：**
```json
{
  "total_snapshots": 100,
  "failed_count": 10,
  "violation_count": 25,
  "recent_failed": [...],
  "size_trend": [
    {
      "date": "2024-01-01 00:00:00",
      "version": "v1.0.0",
      "size": 102400,
      "gzip_size": 30000,
      "project": "项目名"
    }
  ]
}
```

#### 9.4.2 GET /api/dashboard/max-growth

获取增长最快的 Chunk 表表表表。

**Query 参数：**
- project_id: 可选，按项目筛选表表表表

**返回：**
```json
[
  {
    "name": "main.js",
    "current_size": 120000,
    "prev_size": 100000,
    "growth": 20000,
    "version": "v1.1.0",
    "project_name": "项目名"
  }
]
```

## 10. 模块关系与数据流

### 10.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend (React)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────┐  │
│  │  Pages   │  │Components│  │ Context  │  │      Utils      │  │
│  └────┬─────┘  └──────────┘  └────┬─────┘  └─────────────────┘  │
│       │                            │                              │
│       └────────────┬───────────────┘                              │
│                    │                                              │
│              ┌─────┴─────┐                                        │
│              │   Axios   │                                        │
│              └─────┬─────┘                                        │
└────────────────────┼──────────────────────────────────────────────┘
                     │ HTTP/JSON
                     │
┌────────────────────┼──────────────────────────────────────────────┐
│                 Backend (Express)                                 │
│                    │                                              │
│          ┌─────────┴──────────┐                                   │
│          │  Auth Middleware   │ (JWT 校验)                        │
│          └─────────┬──────────┘                                   │
│                    │                                              │
│  ┌─────────────────┼──────────────────────────────────────────┐   │
│  │                 │                                          │   │
│  │   ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐  │   │
│  │   │  Auth   │  │ Projects │  │Snapshots │  │ Dashboard │  │   │
│  │   │ Routes  │  │  Routes  │  │  Routes  │  │  Routes   │  │   │
│  │   └────┬────┘  └────┬─────┘  └────┬─────┘  └─────┬─────┘  │   │
│  └────────┼────────────┼─────────────┼──────────────┼────────┘   │
│           │            │             │              │            │
│           └────────────┴──────┬──────┴──────────────┘            │
│                                │                                   │
│                      ┌─────────┴─────────┐                         │
│                      │   SQLite Database │                         │
│                      └───────────────────┘                         │
└───────────────────────────────────────────────────────────────────┘
```

### 10.2 典型数据流：上传快照

```
用户操作 (ProjectDetail 页面)
    │
    ▼
上传 JSON / 填写信息表表表表
    │
    ▼
前端解析 JSON → 格式化 chunk 数据
    │
    ▼
POST /api/snapshots
    │
    ▼
Auth Middleware (验证 Token)
    │
    ▼
插入 build_snapshots 表
    │
    ▼
循环插入 chunks 表表表表
    │
    ▼
checkBudgetViolations (预算校验)
    │
    ▼
更新 gate_status
    │
    ▼
插入 budget_violations 表表表表
    │
    ▼
返回结果
    │
    ▼
前端刷新快照列表表表表
```

## 11. 注意事项与维护建议

### 11.1 安全相关注意事项

1. **JWT 密钥硬编码问题**
   - 位置：[auth.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/middleware/auth.js) 第 2 行
   - 问题：密钥 `performance-budget-secret-key-2024` 硬编码在代码中
   - 建议：改为从环境变量读取，生产环境使用强密钥

2. **越权访问风险**
   - 问题：快照详情接口 `GET /api/snapshots/:id` 未校验快照所属项目是否属于当前用户
   - 影响：用户可以通过修改 ID 访问其他用户的快照数据
   - 建议：在查询快照时关联 projects 表表表表，校验 `user_id` 是否匹配

3. **SQL 注入风险表表表表**
   - 现状：已使用参数化查询，基本无 SQL 注入风险表表表表
   - 建议：保持使用参数化查询，避免字符串拼接 SQL

### 11.2 代码质量与可维护性

1. **回调嵌套过深（回调地狱）**
   - 位置：[snapshots.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/routes/snapshots.js) 的 POST 接口
   - 问题：多层嵌套回调，可读性差，难以维护
   - 建议：使用 Promise 或 async/await 重构，或使用 SQLite 的 Promise 包装库

2. **代码重复**
   - 位置：[snapshots.js](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/backend/routes/snapshots.js) 第 86-134 行
   - 问题：成功和错误分支有大量重复代码
   - 建议：提取公共逻辑为函数

3. **缺少服务层/控制器层表表表表**
   - 问题：业务逻辑直接写在路由表处理函数中
   - 建议：按 MVC 模式拆分，提取 Service 层和 Controller 层表表表表

4. **缺少错误统一处理表表表表**
   - 问题：每个接口单独处理错误，方式不统一
   - 建议：添加全局错误处理中间件表表表表

### 11.3 功能与体验问题

1. **前端缺少样式表**
   - 问题：代码中使用了大量 Tailwind CSS 类名，但项目未引入 Tailwind CSS
   - 影响：页面样式可能无法正常显示
   - 建议：安装并配置 Tailwind CSS，或改用其他样式方案

2. **Gzip 体积估算表表表表不准确**
   - 位置：[ProjectDetail.jsx](file:///Users/zhangxinyu/sunxidan/9999-gsb/0608/performance-budget-audit/frontend/src/pages/ProjectDetail.jsx) 第 83 行
   - 问题：当 JSON 中没有 gzipSize 表表表表时，使用 `size * 0.3` 估算表表表表
   - 建议：明确告知用户这是估算值，或在后端真实计算 Gzip 体积表表表表

3. **分页功能缺失**
   - 问题：快照列表表表表表、项目列表表表表表等都没有分页，数据量大时会有性能问题
   - 建议：添加分页功能

4. **缺少删除/编辑功能**
   - 问题：项目和快照都没有删除、编辑功能
   - 建议：根据产品需求添加相应功能

### 11.4 性能相关注意事项

1. **SQLite 并发性能**
   - 问题：SQLite 不支持高并发写入
   - 建议：如果用户量增长，考虑迁移到 PostgreSQL 或 MySQL

2. **大表查询性能表表表表**
   - 问题：缺少索引，数据量大时查询会变慢
   - 建议：对常用查询字段添加索引：
     - `projects(user_id)`
     - `build_snapshots(project_id, build_time)`
     - `chunks(snapshot_id)`
     - `budget_violations(snapshot_id)`

3. **前端大数据量渲染表表表表**
   - 问题：Chunk 列表表表表没有虚拟滚动表表表表，数量多时可能卡顿
   - 建议：添加虚拟滚动表表表表或分页

### 11.5 测试与部署建议

1. **缺少自动化测试**
   - 建议：添加单元测试和集成测试

2. **缺少环境变量配置表表表表**
   - 建议：使用 dotenv 管理环境变量（端口、数据库路径、JWT 密钥等）

3. **缺少 CI/CD 配置表表表表**
   - 建议：添加 GitHub Actions 或其他 CI/CD 配置表表表表

### 11.6 后续开发建议

1. **Webpack/Rollup 插件**
   - 建议：开发对应的构建工具插件表表表表，实现自动上传构建快照表表表表

2. **通知/告警功能**
   - 建议：添加邮件、钉钉、企业微信等告警通知表表表表

3. **团队协作功能**
   - 建议：支持团队成员管理、项目权限控制

4. **更多图表类型表表表表**
   - 建议：添加饼图、堆积图等更多可视化方式

5. **导出功能**
   - 建议：支持导出报告、导出数据

## 12. 快速启动

### 12.1 启动后端

```bash
cd backend
npm install
npm run dev
```
服务默认运行在 http://localhost:8038

### 12.2 启动前端

```bash
cd frontend
npm install
npm run dev
```

### 12.3 构建部署

```bash
cd frontend
npm run build

cd ../backend
npm start
```
后端会自动服务前端构建产物。
